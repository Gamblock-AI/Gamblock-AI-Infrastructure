# Gamblock-AI production server.
# Replace with your authorized VPS IP. SSH key defaults to the user-relative
# path configured in ansible.cfg; override it locally, never with a committed
# workstation-specific absolute path.
[gamblock]
43.228.213.38 ansible_user=deployer ansible_port=22
