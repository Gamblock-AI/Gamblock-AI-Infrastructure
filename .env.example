# Local secrets for scripts (gitignored). Copy to .env and fill in.
# Optional: a GitHub PAT used by scripts/github-secrets.sh for auth. If unset,
# the scripts fall back to the gh CLI's stored auth (gh auth login).
# GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
